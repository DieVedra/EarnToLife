using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpritesProvider : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // UpdateActivityFromList is called once per frame
    void Update()
    {
        
    }
}
